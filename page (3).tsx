'use client';import Link from 'next/link';import {useState} from 'react';import {ArrowLeft} from 'lucide-react';import {supabase} from '@/lib/supabase';export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await supabase().auth.signInWithPassword({email,password});setBusy(false);if(r.error){setError(r.error.message);return}location.href='/profile'}return <><Link href="/" className="muted"><ArrowLeft size={16} style={{verticalAlign:'middle'}}/> Back</Link><div className="hero" style={{marginTop:20}}><div className="eyebrow">Welcome back</div><h1>Sign in</h1><form className="form" onSubmit={submit}><input className="input" type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" required placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="notice">{error}</div>}<button className="btn" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></div></>}
